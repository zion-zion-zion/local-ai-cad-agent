# compare_global_properties_rdescriptor

## API Definition

```python
def compare_global_properties_rdescriptor(target: ModelInput, current: ModelInput) -> dict[str, Any]
```

*Source: inspect/brep/diagnostics.py*

## Import Surface

- inspection namespace: `from simplecadapi.inspect import brep` then `brep.compare_global_properties_rdescriptor(...)`; unavailable inside GraphSession/@model

## Description

Compare material counts, mass properties, bounds, and raw topology.
