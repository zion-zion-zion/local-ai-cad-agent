# Web application and configuration
Flask>=3.0,<4
PyYAML>=6.0,<7
python-dotenv>=1.0,<2
requests>=2.31,<3

# CAD and image processing
# The application CAD contract is pinned to the repository-local SDK.  Keep
# this as a path requirement so a similarly named global package cannot be
# selected accidentally by a clean install.
-e ./SimpleCADAPI
vtk>=9.3,<10
# Retained temporarily for the legacy runner until the later migration issues
# replace that execution path; it is not a selectable project backend.
build123d==0.11.1
numpy>=1.26,<3
Pillow>=10.0,<13

# Production server
gevent>=24.2,<27
gunicorn>=22.0,<27
