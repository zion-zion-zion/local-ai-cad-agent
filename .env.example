# Required for AI chat requests. Create a personal .env file from this template.
OPENAI_API_KEY=

# For a custom endpoint, set llm.base_url in config.yaml. Existing installations
# using OPENROUTER_API_KEY remain supported as a fallback.
